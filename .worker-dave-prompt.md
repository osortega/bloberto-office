You are Dave, DevOps Engineer. Do this task in /workspace:

1. Run: git config --global --add safe.directory /workspace

2. Read /workspace/src/Office.jsx. Find where desks are rendered with occupied workers. Each desk has a lamp (SVG ellipse). Currently lamps toggle opacity based on occupied vs vacant.

3. For desks with active (non-ghost) workers, pass the worker's progress (0-100) as a CSS variable on the desk element: add `'--desk-progress': (worker.progress ?? 0) / 100` to the desk's style.

4. In /workspace/src/Office.css, update the desk lamp styles:
- Find the rule for active desk lamps (the ellipse that shows the lamp glow)
- Change it so opacity scales with progress: `opacity: calc(0.35 + var(--desk-progress, 0) * 0.65)`
- Add a drop-shadow that scales: `filter: drop-shadow(0 0 calc(2px + var(--desk-progress, 0) * 5px) #fbbf24)`

RULES: Read the files before editing. Make minimal changes. Keep it clean.
